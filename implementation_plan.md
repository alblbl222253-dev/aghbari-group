# خطة تطوير وترقية الموقع (المرحلة الثانية - التوسعة وتحسين الألوان)

ملاحظاتك ممتازة وفي محلها تماماً. اللون الأبيض الساطع في الوضع الفاتح قد يقلل من تباين النصوص وتأثير الزجاج. كما أن تطبيق الترجمة يجب أن يشمل جميع أجزاء الصفحة وجميع الواجهات.

## User Review Required
> [!IMPORTANT]
> يرجى مراجعة هذه الخطة لإكمال تطبيق اللمسة الاحترافية وتأكيدها.

## Proposed Changes

### 1. تحسين ألوان الوضع الفاتح (Light Mode Revamp)
بناءً على اقتراحك، سنبتعد عن اللون الأبيض الصارخ وسنعتمد على درجات "الرصاصي الفاتح / البيج" لتكون الخلفيات مريحة للعين وتعطي تبايناً أقوى مع النصوص والأيقونات.
- #### [MODIFY] `website/css/style.css`
  تعديل `[data-theme="light"]` ليشمل:
  - `--color-bg-primary`: لون بيج/رصاصي مريح (مثل `#F5F5F0` أو `#F0F0ED`).
  - `--color-text-primary`: أسود فاحم لزيادة الوضوح والقضاء على البهتان.
  - `--glass-border` و `--glass-bg`: سيتم تعميقها لتبرز حاويات البطاقات (Containers) والأيقونات بشكل أقوى.

---

### 2. توسيع نظام الترجمة لجميع الواجهات (Full i18n Coverage)
- #### [MODIFY] `website/js/i18n.js`
  توسيع قاموس الترجمة ليضم نصوص أقسام (الخدمات، الإحصائيات، معرض الأعمال، من نحن، تواصل معنا) بدلاً من الهيدر فقط.
- #### [MODIFY] `website/index.html`
  إضافة وسوم `data-i18n` لجميع العناوين، النصوص، والأزرار في الصفحة الرئيسية بالكامل.
- #### [MODIFY] بقية الصفحات (`services.html`, `portfolio.html`, `about.html`, `contact.html`, `blog.html`, `faq.html`)
  - نسخ أزرار التبديل (المظهر واللغة) وإدراجها في شريط التنقل لكل صفحة.
  - استدعاء ملف `i18n.js` في كل الصفحات.
  - إضافة `data-i18n` للنصوص المشتركة (كالهيدر والفوتر).

## Verification Plan
1. **التباين**: سنتأكد من أن النصوص الداكنة تقرأ بوضوح شديد على الخلفية البيج/الرصاصية الجديدة للوضع الفاتح.
2. **الشمولية**: عند الضغط على زر (EN)، سنراقب تحول كامل محتوى الصفحة الرئيسية (وليس فقط الرأس) إلى الإنجليزية.
3. **التنقل**: سنختبر فتح صفحة (من نحن) والتأكد من وجود زر التبديل واحتفاظها بخيار اللغة المختار مسبقاً.
